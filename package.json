{
  "private": true,
  "name": "marissa-bible",
  "homepage": "https://marissa-bible-kento.netlify.com",
  "scripts": {
    "deploy": "netlify deploy --prod --dir=public",
    "postdeploy": "echo 'Your site was deployed to https://marissa-bible-kento.netlify.com'",
    "start": "browser-sync start --server public --files public"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "stylelint": "^10.0.1",
    "stylelint-config-standard": "^18.3.0"
  }
}
